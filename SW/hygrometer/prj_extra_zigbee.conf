# Kconfig overlay for the Zigbee (ZBOSS / ncs-zigbee add-on) variant.
# Replaces the default BTHome BLE build. Requires NCS v3.3.0 + the ncs-zigbee add-on.
# Usage: west build -- -DEXTRA_CONF_FILE=prj_extra_zigbee.conf \
#          -DSB_EXTRA_CONF_FILE=sysbuild_extra_zigbee.conf \
#          -DPM_STATIC_YML_FILE=$(pwd)/pm_static_zigbee.yml

CONFIG_APP_USE_ZIGBEE=y

# Zigbee R23 stack as a sleepy end device.
# (NCS_SAMPLES_DEFAULTS is intentionally not used — it enables -Werror, which trips
# on benign warnings in the precompiled ZBOSS macros during bring-up.)
CONFIG_COMPILER_WARNINGS_AS_ERRORS=n
CONFIG_ZIGBEE_ADD_ON=y
CONFIG_ZIGBEE_APP_UTILS=y
CONFIG_ZIGBEE_ROLE_END_DEVICE=y
CONFIG_ZIGBEE_CHANNEL_SELECTION_MODE_MULTI=y

# ZBOSS needs more heap/stacks than the BTHome build
CONFIG_HEAP_MEM_POOL_SIZE=4096
CONFIG_MAIN_STACK_SIZE=4096
CONFIG_SYSTEM_WORKQUEUE_STACK_SIZE=2048

# Zigbee uses the 802.15.4 radio — disable BLE and the BLE-only MCUmgr/DFU stack.
CONFIG_BT=n
CONFIG_BT_PERIPHERAL=n
CONFIG_MCUMGR=n
CONFIG_MCUMGR_TRANSPORT_BT=n
CONFIG_IMG_MANAGER=n
# No OTA/MCUboot in this first cut (single-image, see sysbuild_extra_zigbee.conf)
CONFIG_BOOTLOADER_MCUBOOT=n

# No UART on the hygrometer board; debug over RTT via prj_extra_rtt.conf
CONFIG_SERIAL=n
CONFIG_CONSOLE=n
CONFIG_UART_CONSOLE=n

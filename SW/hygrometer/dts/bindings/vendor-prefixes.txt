# Vendor prefix for this project's own (non-vendor) devicetree nodes — the
# local,* compatibles (leak-sensor, adc-vbatt, bl54l15u-*). Registering it
# silences the "unknown vendor prefix" warning, which Twister promotes to a
# hard error (it builds with --edtlib-Werror). This copy covers the native_sim
# unit test and the hygrometer app image; the per-board copies under
# boards/local/*/dts/bindings cover the firmware app + mcuboot images.
local	Local

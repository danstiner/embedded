# Sysbuild overlay for the Zigbee variant.
# First cut is single-image (no MCUboot/OTA); ZBOSS NVRAM is carved out by
# pm_static_zigbee.yml. OTA over Zigbee is a follow-up.
# BOOTLOADER is a choice — select "none" rather than disabling MCUboot.
SB_CONFIG_BOOTLOADER_NONE=y

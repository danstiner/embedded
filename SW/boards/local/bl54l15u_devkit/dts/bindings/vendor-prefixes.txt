# Register the "local" vendor prefix for this board's local,bl54l15u-devkit
# node. BOARD_DIR is in DTS_ROOT for every image built for this board, so this
# covers the app and mcuboot builds.
local	Local
